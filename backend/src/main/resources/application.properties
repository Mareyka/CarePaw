spring.application.name=animals
server.port=8080

# ???? ?????? (MySQL)
spring.datasource.url=jdbc:mysql://localhost:3306/care_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=12345
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# ???????? ??????
file.upload.dir=uploads/
file.upload.posts-dir=uploads/images/posts/
file.upload.avatars-dir=uploads/images/profiles/
file.upload.pets-dir=uploads/images/pets/

spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# ??????????? ???????
spring.web.resources.static-locations=file:${file.upload.dir}

# CORS
app.cors.allowed-origins=http://localhost:8081