spring:
  application:
    name: animals
  datasource:
    url: jdbc:mysql://localhost:3306/carepaw_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: 12345
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false

server:
  port: 8080

app:
  cors:
    allowed-origins: "http://localhost:8081"
